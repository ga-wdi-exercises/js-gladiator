
class Arena {
  constructor (name) {
    this.name = name.charAt(0).toUpperCase() + name.slice(1)
    this.gladiator = []


  }
}
var colosseum = new Arena("megalopolis")
var newGladiator = new gladiator("Maximus")


if (weapon === 'spear' || weapon === 'club' || weapon === 'Trident') {
  this.weapon = weapon
} else {
}
}
