class Arena extends Gladiator {
  constructor(name){

    this.name = name;
  }
  super(name, weapon)
  gladiators(Gladiator){
var glads[];
push
  }
  addGladiator(Gladiator){
    var glads[];
    var weaps[];
    glads.push(this.name);
    weaps.push(this.weapon);
  }

}
