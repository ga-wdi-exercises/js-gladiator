class Arena {
  constructor (name) {
    this.name = name.charAt(0).toUpperCase() + name.slice(1);
    this.gladiator = [];
    }
  }

addGladiator (glad) {
  if (this.gladiators.length < 2) {
    this.gladiators.push(glad)
  }else{
    console.;log('Too many gladiators in the ring')
  }
}
var colosseum = new Arena ("Verona")
